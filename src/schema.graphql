type User {
  isLoggedIn: Boolean!,
  name: String
  callname: String
  email: String
}

type Query {
  hello: String
  user: User
}

type Mutation {
  setName(name: String, callname: String): Boolean
}

schema {
  query: Query
  mutation: Mutation
}
