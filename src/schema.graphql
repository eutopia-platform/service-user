type User {
  isLoggedIn: Boolean!
  id: ID!
  name: String!
  callname: String!
  email: String!
}

type Query {
  hello: String
  user: User
  users(uids: [ID!]!): [User!]!
}

type Mutation {
  setName(name: String, callname: String): User
}

schema {
  query: Query
  mutation: Mutation
}
