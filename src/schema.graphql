type User {
  isLoggedIn: Boolean!,
  name: String
  callname: String
  email: String
}

type Query {
  hello: String
  user: User
}

type Mutation {
  logout: Int
}

schema {
  query: Query
  mutation: Mutation
}
